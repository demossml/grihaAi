# FIX: parseTotalFromText R1.1 (сдача / скидка на итог / OCR «ОГ»)

Дата: 2026-09-12 (repo: grihaAi, ветка main)
Спека: TZ-fix-parser-total.md (R1.1)
Файлы: `apps/agent/src/services/documents/extractors/parsers.ts`,
`tools/backfill-totals/backfill.ts` (без изменений), `tests/unit/documents-parsers.test.ts`.

## Что сделано

`parseTotalFromText` переписан пошагово (приоритет фиксирован, сигнатура не менялась):

1. **Построчно** «ИТОГ/ИТОГО/всего к оплате/к оплате/total due/grand total» + число;
   строки `isNoise` (`ндс|нас\b|налог|скидк|процент|\b%\b`) пропускаются —
   «Скидка на итог 0.07» больше не может стать total.
2. OCR-обрезки итога: `^(ог|того|ит)\s*[:=]\s*число$` → «ОГ =2437» работает.
3. Осторожный `сумма\s*[:=]\s*число` на не-noise строке («СУММА НДС», «ИТОГО ДО
   СКИДОК» отсекаются isNoise).
4. НАЛИЧНЫМИ/БЕЗНАЛИЧНЫМИ/оплачено — **только если** в тексте нет `сдача\s*[:=]?\s*\d`
   (внесённая сумма > итога при сдаче).
5. Fallback: последнее число с `₽/руб` на не-noise строках.

## Dry-run / apply

- Локальной производственной `~/.grish-ai/documents.sqlite` нет (реальная БД —
  `/home/admingimolost/.grish-ai/documents.sqlite` на сервере).
- **Production `--apply` НЕ выполнялся** (правило 7: запрещён до сверки dry-run с
  эталонами на реальной БД).
- Smoke-проверка скрипта на временной БД с эталонными raw_text §1:

| id (prefix) | dry-run → | эталон §1 | статус |
|---|---|---|---|
| 3378160a | **2437** (не 3037) | 2437 | ✅ |
| 4858c373 | **20515** (не 0.07) | 20515 | ✅ |
| f8247c3a | **175** | 175 | ✅ |
| 11f7678e | **6767** | 6767 | ✅ |
| 83b854f2 | **7667.5** | 7667.5 | ✅ |

Сумма группы «Ремонт» 39 561,52 ₽ — проверить на сервере после apply полных данных.

## Тесты

`documents-parsers.test.ts` + 5 новых кейсов R1.1:
- «Скидка на итог 0.07» → 20515 по «ИТОГ: 20515»;
- ИТОГ 2437, не НАЛИЧНЫМИ 3037 при СДАЧА; без ИТОГ + СДАЧА → undefined;
- «ОГ =2437» + «СУММА БЕЗ НАС»;
- «НДС 22%» не побеждает «ИТОГ 96»;
- регрессия «сумма: 99.90 руб» → 99.9.

Старые позитивные кейсы зелёные. **Итог: 692 unit-теста PASS** (было 687),
`turbo typecheck` 10/10.

## Next (на сервере)

1. `npx tsx tools/backfill-totals/backfill.ts ~/.grish-ai/documents.sqlite` — dry-run;
2. сверить §1 эталоны (3378160a→2437, 4858c373→20515, f8247c3a→175, 11f7678e→6767, 83b854f2→7667.5);
3. при 0 расхождений: `… --apply`;
4. проверить сумму группы «Ремонт» = 39 561,52 ₽.
