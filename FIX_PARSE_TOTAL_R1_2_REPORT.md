# FIX: parseTotalFromText R1.2 — «ИТОГО……6767» (точки-заполнители)

Дата: 2026-09-12 (repo: grihaAi, ветка main)
Файл: `apps/agent/src/services/documents/extractors/parsers.ts` (шаг 1 функции),
`tests/unit/documents-parsers.test.ts`.

## Правка

Шаг 1 `parseTotalFromText`: между маркером итога и числом теперь разрешён filler
из пробелов, точек, дефисов, `:` и `=`:

```ts
/(?:итого|итог|всего\s+к\s+оплате|к\s+оплате|total\s+due|grand\s+total)
 (?=[\s.:=\-–—\d]|$)[\s.:=\-–—]*([\d][\d\s\u00a0]*(?:[.,]\d{1,2})?)/i
```

- «ИТОГО.........................................6767.00» → **6767** (эталон 11f7678e).
- Десятичная часть (6767.**00**) не ломается: filler не поглощает цифры.

### Обнаруженный попутно баг границ

Исходный паттерн использовал `\b` после кириллического маркера. В JS `\b`
работает только по ASCII `\w` — после кириллицы границы нет вообще, поэтому
`итого\b...` **никогда** не матчился. Заменено на lookahead
`(?=[\s.:=\-–—\d]|$)`. Аналогично починено в `isNoiseLine`:
`(?:ндс|нас)(?=[\s\d%]|$)` и `%` вместо `\b%\b` (раньше «НДС 22%» не отсекалась
границей и отсекалась только порядком шагов — теперь честно).

Правка глобальная — без chat-specific веток.

## Dry-run / apply

- Локальной production-БД `~/.grish-ai/documents.sqlite` нет (реальная — на
  сервере `/home/admingimolost/.grish-ai/documents.sqlite`).
- **Production `--apply` НЕ выполнялся** (правило: apply только после сверки
  dry-run с эталонами на реальной БД).
- Smoke dry-run на временной БД с эталонными raw_text §1 (R1.1) + §1 (R1.2):

| id (prefix) | dry-run → | эталон | статус |
|---|---|---|---|
| 3378160a | 2437 (не 3037) | 2437 | ✅ (R1.1 не сломан) |
| 4858c373 | 20515 (не 0.07) | 20515 | ✅ (R1.1 не сломан) |
| f8247c3a | 175 | 175 | ✅ (R1.1 не сломан) |
| **11f7678e** | **6767** (было бы 22/undefined) | **6767** | ✅ (R1.2) |
| 83b854f2 | 7667.5 | 7667.5 | ✅ |

## Тесты

Добавлены в `documents-parsers.test.ts`:
- `ИТОГО.........................................6767.00` → 6767;
- `ИТОГ...........175.00` → 175.

R1.1-кейсы зелёные. **Итог: 694 unit-теста PASS** (было 692), `turbo typecheck` 10/10.

## Next (на сервере)

1. `npx tsx tools/backfill-totals/backfill.ts ~/.grish-ai/documents.sqlite` — dry-run;
2. сверить 11f7678e → 6767 и регрессии R1.1;
3. при 0 расхождений: `… --apply`.
